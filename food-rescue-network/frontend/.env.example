# Leave this unset when the frontend is served by the same Render service as
# the Express API. For a separate frontend host, use the complete API URL:
# VITE_API_URL=https://rescue-food.onrender.com/api
